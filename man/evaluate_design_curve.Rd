% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve.R
\name{evaluate_design_curve}
\alias{evaluate_design_curve}
\title{Operating characteristic curve for a two-stage design}
\usage{
evaluate_design_curve(n1, n, r1, e1, r, p)
}
\arguments{
\item{n1}{Integer. Stage-1 sample size.}

\item{n}{Integer. Total sample size (\code{n > n1}).}

\item{r1}{Integer. Futility boundary: stop for futility if X1 <= r1.
Use \code{-1} for no futility stopping.}

\item{e1}{Integer. Interim efficacy boundary: declare success if X1 >= e1.
Use \code{n1 + 1} for no interim efficacy stopping.}

\item{r}{Integer. Final success threshold: success if X1 + X2 >= r.}

\item{p}{Numeric vector of response probabilities in (0, 1).}
}
\value{
A \code{data.frame} with one row per element of \code{p} and columns:
\describe{
\item{p}{Response probability.}
\item{p_success}{Probability of trial success (early efficacy or X1 + X2 >= r).}
\item{p_futility}{Probability of early stopping for futility (X1 <= r1).}
\item{p_efficacy1}{Probability of early stopping for efficacy at the interim
analysis (X1 >= e1).}
\item{en}{Expected sample size.}
}
}
\examples{
evaluate_design_curve(n1 = 15L, n = 25L, r1 = 0L, e1 = 9L, r = 5L,
                      p = seq(0.05, 0.50, by = 0.05))
}
\description{
Evaluates the operating characteristics of a two-stage design at each
element of a user-supplied vector of response probabilities \code{p}.
This enables operating-characteristic curves across a range of \code{p} values.
}
